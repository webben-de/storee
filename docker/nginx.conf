server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # nginx returns 405 for POST to static files — serve the SPA instead
  error_page 405 =200 /index.html;
}
